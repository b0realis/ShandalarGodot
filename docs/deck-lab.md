# Deck Lab

Moved. The headless AI-vs-AI deck testing harness now lives in its own
folder at the repository root, with its CLI, its documentation and its
results together:

- **[`DeckLab/README.md`](../DeckLab/README.md)** — the manual
- `DeckLab/deck_lab.sh` — the command-line tool
- `DeckLab/results/` — where a run writes its statistics

This stub stays because the Deck Lab is cited by file name across the
docs and in commit history, and a dead link is worse than a redirect.
