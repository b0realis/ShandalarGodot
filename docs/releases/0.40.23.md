# Shandalar 0.40.23

- After a Deck Lab run's last game, the aggregate scanned the whole task
  list once per matchup — nothing for a duel, 18.5 billion dictionary
  reads for a thousand-deck tournament, twenty-two minutes at 100% with
  nothing to tell it from a hang. Records are grouped by matchup in one
  pass now, and the tournament tables read the matchup list once. The
  report is byte for byte what it was (checked on a 3,440-game
  tournament: `matchups.csv`, `standings.csv`, `top.txt`).
- No wire change: protocol 24 peers interoperate.
