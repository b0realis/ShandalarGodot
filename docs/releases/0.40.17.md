# Shandalar 0.40.17

- The computer opponent reads Aggression (Ice Age) as the owner ruled it:
  removal first, a gift second.
  - Across the table, on a creature the clause actually destroys: one
    that cannot attack at all, or one an untapped creature of ours can
    block, survive (it will have first strike) and kill. A creature that
    walks through our board every turn is not offered the aura — it
    would only arm it.
  - On our own board only in the first main phase, on a creature of four
    power or more that lacks trample, and only when the declaration
    itself says the creature would attack once it has the gifts. A 2/2,
    a summoning-sick body, a trampler already, or a second main phase
    keeps the aura in hand.
  - A creature of ours that wears one — ours or theirs — attacks whatever
    the analysis says: staying home is a destroy at our own end step,
    before it could block anything.
  - "Enchanted creature has first strike and trample" is read as both
    grants (the reader saw only the first); Wings of Aesthir's "flying
    and first strike" likewise. Base-pool readings are unchanged.
- No wire change: protocol 24 peers interoperate.
