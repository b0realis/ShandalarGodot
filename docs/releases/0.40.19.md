# Shandalar 0.40.19

- The local Linux build ships `auto_deck.sh` beside `deck_lab.sh`, as the
  packaged releases already did; the play copy can mine a field and play
  it without a checkout.
- No wire change: protocol 24 peers interoperate.
