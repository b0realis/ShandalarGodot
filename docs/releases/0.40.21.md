# Shandalar 0.40.21

- The AutoDeck CLI started from a checkout (`DeckLab/auto_deck_cli.sh`)
  failed to compile in 0.40.20: its new set → pack refusal named the
  `CardPacks` autoload, which a `--script` run compiles before the
  autoloads exist (the packaged `auto_deck.sh`, which loads the tool
  inside the running game, was not affected). The table is reached
  through the script now, and `tools/test_script_entry_points.py`
  holds every `--script` entry point to naming no autoload.
- On a play copy, `--gauntlet decks/` (and `--matrix decks/`) found no
  folder: the shipped library is inside the `.pck`, which only
  `DirAccess` sees. The Lab reads it there now, so the packaged
  `deck_lab.sh` plays a tournament against the library as the checkout
  does.
- No wire change: protocol 24 peers interoperate.
